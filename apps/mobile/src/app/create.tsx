import { SectionPlaceholder } from "@/components/section-placeholder";

export default function CreateScreen() {
  return (
    <SectionPlaceholder
      eyebrow="CAPTURA RÁPIDA"
      title="Crear"
      description="Registra una sesión, una reflexión, una meta o una evidencia sin perder el contexto."
    />
  );
}
