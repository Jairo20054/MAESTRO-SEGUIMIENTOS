import { SectionPlaceholder } from "@/components/section-placeholder";

export default function ProfileScreen() {
  return (
    <SectionPlaceholder
      eyebrow="TU ESPACIO"
      title="Perfil"
      description="Preferencias, privacidad, exportaciones y control de la futura sincronización."
    />
  );
}
